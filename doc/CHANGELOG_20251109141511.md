# CHANGELOG - 2025-11-09 14:15:11

## 変更内容

### ファイル構成の整理

プロジェクトのファイル構成を整理し、各ファイルを適切なディレクトリに配置しました。

#### ファイル移動

1. **スクリプトファイルをmini_muse/に移動**
   - `generate_images.py` → `mini_muse/generate_images.py`
   - `batch_video_generation.py` → `mini_muse/batch_video_generation.py`

2. **テストファイルをtests/に移動**
   - `test_placeholder_resolution.py` → `tests/test_placeholder_resolution.py`
   - `test_video_generation.py` → `tests/test_video_generation.py`

#### 新規追加ファイル

- `prompts/prompt_templates_モンスターの絵_cthulhu_mythos_beksinski_20251030.json`
- `prompts/prompt_templates_統計ML可視化_20250107.json`

#### ドキュメント更新

- `CLAUDE.md` を最新のファイル構成に合わせて更新
  - ディレクトリ構造図を更新
  - 実行コマンド例のパスを修正（`mini_muse/generate_images.py`など）
  - 最終更新日を 2025-11-09 に更新
  - batch_video_generation.py の説明を追加
  - tests/ の一覧を更新

### 移動後のディレクトリ構造

```
/home/perso/analysis/mini-muse/
├── mini_muse/                    # メインパッケージ
│   ├── comfyui_client.py
│   ├── prompt_generator.py
│   ├── ollama_prompt.py
│   ├── generate_images.py        # ← 移動
│   └── batch_video_generation.py # ← 移動
├── tests/                        # テストスクリプト
│   ├── test_comfyui_client.py
│   ├── test_prompt_generator.py
│   ├── test_integration.py
│   ├── test_generate.py
│   ├── test_simple_generate.py
│   ├── test_ollama_video_prompt.py
│   ├── test_placeholder_resolution.py  # ← 移動
│   └── test_video_generation.py        # ← 移動
```

### 実行方法の変更

スクリプトのパスが変更されたため、以下のように実行してください：

```bash
# 画像生成（旧）
# uv run python generate_images.py

# 画像生成（新）
uv run python mini_muse/generate_images.py

# 動画生成（旧）
# uv run python batch_video_generation.py

# 動画生成（新）
uv run python mini_muse/batch_video_generation.py
```

## 目的

- プロジェクト構成の整理
- ファイルの所在を明確化
- メインパッケージ（mini_muse/）とテスト（tests/）の分離
- ドキュメントの最新化

## 影響範囲

- スクリプト実行時のパス指定が変更
- インポート文への影響なし（mini_muse パッケージとして機能）
- 既存のテストファイルは影響なし
